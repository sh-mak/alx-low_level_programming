#include "main.h"
/**
 * mul - multiplies two integers
 * @a: the 1st number to multiply
 * @b: the 2nd number to multiply with the 1st
 * Return the resault of the multiplication between a & b
 */
int mul(int a, int b)
{
	return (a * b);
}
