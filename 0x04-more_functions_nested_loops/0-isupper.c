#include "main.h"
/**
 * _isupper - checks for uppercase character
 * @x: the check  number
 * Return: 1 for upper letter and 0  for else
 */
int _isupper(int c)
{
	if (x >= 65 && x <= 90)
	{
		retur (1);
	}
	return (0);
}
